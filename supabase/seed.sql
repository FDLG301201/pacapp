-- Seed data for local development only
-- Run after migrations with: supabase db reset
-- NOTE: auth.users entries must be created via Supabase dashboard or API;
--       these profiles assume the UUIDs already exist in auth.users.

-- Example: update an existing user to admin role
-- update public.profiles set role = 'admin' where id = '<your-user-uuid>';
